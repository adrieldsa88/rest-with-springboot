spring:
  application:
    name: rest-with-spingboot-and-java-erudio

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spingboot_erudio?useTimezone=true&serverTimezone=UTC
    username: root
    password: adriel300

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:

    show-sql: false

    open-in-view: false


